const SSC = require('./ssc');

const test = new SSC('https://steemsmartcontracts.tk:5000');

test.getBlockInfo(2, (err, result) => {
    console.log(err, result);
})

/*test.stream((err, res) => {
    console.log(err, res);
})



*/